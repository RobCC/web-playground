import React from "react";
import PropTypes from "prop-types";

import Input from "../../components/Input/Input";

const Home = () => {
  return (
    <div>
      Home page.
      <Input type="text" />
    </div>
  );
};

export default Home;
